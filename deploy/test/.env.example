# deploy/test/.env.example —— 复制为 .env 并填值(.env 已被 .gitignore 忽略,勿入库)
# Keycloak 管理员(首次启动引导用,登录后建议改强密码)
KC_ADMIN_USER=admin
KC_ADMIN_PASSWORD=change-me-strong

# Keycloak 的 Postgres 后端
KC_DB_NAME=keycloak
KC_DB_USER=keycloak
KC_DB_PASSWORD=change-me-strong-db
